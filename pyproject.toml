[project]
name = "nutrigo-ai"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "pydantic>=2.8",
]

[project.optional-dependencies]
ingest = [
  "httpx>=0.27",
  "tenacity>=8.2",
  "playwright>=1.47",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
 